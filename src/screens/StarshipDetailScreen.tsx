import { View } from "react-native";
import { Text, Chip, DataTable, List } from "react-native-paper";

export const StarshipDetailScreen = () => {
  return (
    <View>
      <Text>Test</Text>
    </View>
  );
};
